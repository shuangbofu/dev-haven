# 数据与安全边界

项目和文档保存在用户指定目录，集中记忆保存元数据、变更、扫描输入、Agent 输出、报告及包含正文的本地搜索索引。这些文件和诊断日志可能包含个人路径、源码、文档正文或内部信息，不应随源码或安装包发布。

全文搜索在本地执行。启用 Codex 扫描或报告会把对应样本和记录交给用户配置的 Codex CLI，其模型服务、登录与网络行为由该工具配置决定；本地存储不代表扫描内容始终留在设备。Git 与环境工具下载同样可能访问网络。

终端运行用户权限下的命令。元数据本身不自动执行启动配置；Markdown 和源码阅读不执行文档代码，HTML 阅读会在隔离帧中执行内嵌脚本。排除路径和符号链接检查用于限制目录访问，不是针对恶意本机程序的安全沙箱。

发布前使用 `npm run release:source` 生成白名单源码目录并检查扫描结果。自动检查只覆盖常见敏感信息模式，发布者仍须审阅新增文件和图片。打包采用独立暂存目录，移除源码映射、个人配置和测试产物。

发现漏洞时，请通过仓库维护者提供的私密渠道联系；托管平台支持时使用私密漏洞报告。公共 Issue 请不要粘贴密钥、真实用户数据或可利用的完整凭据。项目尚无公开安全响应时限承诺。

HTML 阅读使用不带 `allow-same-origin` 的 iframe sandbox，仅允许脚本与本地表单交互。文档无法访问父页面、DevHaven 接口、Node.js 或应用存储；内容安全策略禁止外部资源、网络请求、嵌套页面和表单外发，桌面层拦截跨页导航，沙箱禁止弹窗、下载与顶层导航。自包含 HTML 的内嵌脚本、事件处理器、样式及 data 资源可以运行；不为文档开放本地相邻文件访问。脚本仍会消耗渲染进程资源，隔离不保证恶意页面不会导致卡顿。

stdio MCP 仅提供查询，复用索引，不监听网络端口。更新检查仅向发布者随应用内置的仓库服务读取公开 Release 元信息，不下载或执行安装程序。
